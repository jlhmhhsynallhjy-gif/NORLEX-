class AppConstants {
  static const String appName = 'NORLEX';
  static const int maxFileSizeMb = 100;
  static const int maxUploadRetries = 3;
  static const Duration defaultAnimationDuration = Duration(milliseconds: 300);
}
